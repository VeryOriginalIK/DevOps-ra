name: Build and Push Docker images

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write
  packages: write

concurrency:
  group: build-and-push-images-${{ github.ref }}
  cancel-in-progress: true

env:
  K8S_NAMESPACE: voros-bence

jobs:
  build-and-push:
    name: Build and push images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [backend, frontend]
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Prepare image variables
        id: vars
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            TAG="${GITHUB_REF_NAME}"
          else
            TAG="${GITHUB_SHA::7}"
          fi

          # Repo owner (lowercased for Docker registry compatibility)
          REPO_OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')

          # Only build backend and frontend images
          SERVICES=("backend" "frontend")

          MATRIX_JSON=$(printf '%s\n' "${SERVICES[@]}" | jq -R . | jq -cs .)

          echo "matrix=$MATRIX_JSON" >> "$GITHUB_OUTPUT"
          echo "image_tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "repo_owner=$REPO_OWNER_LOWER" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCT_TOKEN }} 

      - name: Build, tag, and push images
        run: |
          SERVICE=${{ matrix.service }}
          TAG=${{ steps.vars.outputs.image_tag }}
          OWNER=${{ steps.vars.outputs.repo_owner }}
          
          # Set the registry to GHCR
          REGISTRY=ghcr.io/$OWNER
          
          IMAGE=$REGISTRY/calculator-$SERVICE:$TAG
          IMAGE_LATEST=$REGISTRY/calculator-$SERVICE:latest
          
          docker build -t $IMAGE -t $IMAGE_LATEST $CONTEXT
          docker push $IMAGE
          docker push $IMAGE_LATEST
          SERVICE=${{ matrix.service }}
          TAG=${{ steps.vars.outputs.image_tag }}
          if [ "$SERVICE" = "frontend" ]; then
            REGISTRY=calculator.jcloud.jedlik.cloud
            CONTEXT=./calculator
          else
            REGISTRY=calculator_backend.jcloud.jedlik.cloud
            CONTEXT=./calculator_backend
          fi

  verify-pull-secret:
    name: Verify/Create GHCR imagePullSecret
    needs: [build-and-push]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: Install kubectl CLI
        uses: azure/setup-kubectl@v4

      - name: Decode KUBECONFIG
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.JEDLIK_K8S_CONFIG }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Prepare deploy variables
        id: vars
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            TAG="${GITHUB_REF_NAME}"
          else
            TAG="${GITHUB_SHA::7}"
          fi
          OWNER="$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')"
          echo "image_tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "repo_owner=$OWNER" >> "$GITHUB_OUTPUT"

      - name: Update k8s manifests with new image tag
        run: |
          OWNER="${{ steps.vars.outputs.repo_owner }}"
          TAG="${{ steps.vars.outputs.image_tag }}"

          # Update backend, frontend, manifests.
          sed -i -E "s|(ghcr.io/${OWNER}/calculator-backend):[a-zA-Z0-9_.-]+|\\1:${TAG}|g" deployment/calculator/compose.yaml
          sed -i -E "s|(ghcr.io/${OWNER}/calculator-frontend):[a-zA-Z0-9_.-]+|\\1:${TAG}|g" deployment/calculator-backend/compose.yaml

      - name: Ensure/refresh ghcr-pull-secret
        run: |
          export KUBECONFIG=$HOME/.kube/config
          kubectl create secret docker-registry ghcr-pull-secret \
            --docker-server=ghcr.io \
            --docker-username="${{ steps.vars.outputs.repo_owner }}" \
            --docker-password="${{ secrets.ACCESS_TOKEN || github.token }}" \
            --docker-email=actions@github.com \
            --dry-run=client -o yaml | kubectl apply -n "$K8S_NAMESPACE" -f -

      - name: Commit and push manifest changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "ci: update k8s manifests to image tag ${{ steps.vars.outputs.image_tag }}"
          branch: ${{ github.ref_name }}
          file_pattern: "deployment/backend/backend.yaml deployment/frontend/frontend.yaml deployment/backend/database.yaml"

      - name: Deploy to Kubernetes
        run: |
          set -euo pipefail
          export KUBECONFIG=$HOME/.kube/config

          kubectl apply -f deployment/backend/backend.yaml
          kubectl apply -f deployment/frontend/frontend.yaml
          kubectl apply -f deployment/backend/database.yaml

          kubectl rollout status deployment/vizsgaremek-etterem-backend -n "$K8S_NAMESPACE" --timeout=300s &
          p1=$!
          kubectl rollout status deployment/vizsgaremek-etterem-frontend -n "$K8S_NAMESPACE" --timeout=300s &
          p2=$!
          kubectl rollout status deployment/vizsgaremek-etterem-adatbazis -n "$K8S_NAMESPACE" --timeout=300s &
          p3=$!

          wait $p1
          wait $p2
          wait $p3
